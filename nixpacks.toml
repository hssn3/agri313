[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = [
  "npm run build",
  "npx tsc -p tsconfig.server.json"
]

[start]
cmd = "node dist-server/server/index.js"
